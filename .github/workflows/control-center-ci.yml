name: Control Center CI

on:
  pull_request:
    paths:
      - "apps/control-center/**"
      - ".github/workflows/control-center-ci.yml"
  push:
    branches: [main]
    paths:
      - "apps/control-center/**"
      - ".github/workflows/control-center-ci.yml"

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/control-center

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            apps/control-center/requirements.txt
            apps/control-center/requirements-dev.txt

      - name: Install dependencies
        run: python -m pip install --upgrade pip && pip install -r requirements-dev.txt

      - name: Capture lint autofix diff
        run: ruff check --fix --diff . > ruff-fix-diff.txt 2>&1 || true

      - name: Upload lint autofix diff
        uses: actions/upload-artifact@v4
        with:
          name: maintenance-ruff-fix-diff
          path: apps/control-center/ruff-fix-diff.txt

      - name: Fail on lint findings
        run: ruff check .

      - name: Test
        run: pytest

      - name: Compile Python sources
        run: python -m compileall -q .
